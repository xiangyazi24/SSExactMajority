# 2026-05-13 早安 handoff（夜里 Claude Code 自主跑）

## 起因

昨天下午（5/12）那批 22 个 commit 是用 Opus 4.6 做的。她自己在 LSP 看是绿的，所以提交了。但 `.olean` 是 5/10 18:53 那次留下的缓存——之后的所有 commit 都没有真正从源码重新编译验证过。从 `2d104461` 这个 HEAD 跑 `lake build SSExactMajority.Convergence.BurmanProof` 是不过的。

我（夜里这个 Claude Code session，Opus 4.7）做了什么、剩下什么，写在这里给早上的你。

## 我修了什么（commit `dc37c600`）

### Bug A：`phase3a_to_awakening` 三处未定义 `hFollowerState`

`c0db2577` "PROVE phase3a_to_awakening FULLY!" 那个 commit 引入的。`leader_low_dt` helper 内部和 `follower-dt≤1` 二步分支，写了 `exact hFollowerState w hwF`——但这函数顶端只 `obtain ⟨hRes, hRc, hUniq, hLorF⟩ := hDormant`，scope 里根本没 `hFollowerState`。那是隔壁 `phase3bc_from_awakening` 解构 `IsAwakeningConfig` 时用的名字，4.6 复制 follower-处理代码时变量名忘改了。

修：line 883 和 line 1190 两处改成 `exact Or.inr ⟨hRes w, hRc w⟩`（IsDormantConfig 给出全员 Resetting + rc=0，直接构造 Or.inr 分支）。Line 1063 那处在 `/- OLD inl proof ... -/` block 里，是死代码，不动。

### Bug B：`propagateReset_recruits` line 217 omega 回归

`e3a8badc` "extract processAgent from propagateReset" 把 `processAgent` 从 `propagateReset` 内部的 inline `let` 抽成 file-level `def`。之前 `unfold propagateReset` 能把 nested `let processAgent` 一起展开，老的证明结尾 `split_ifs <;> simp_all [resetOSSR] <;> omega` 才能跑通。抽出后 unfold 进不去 def 体，omega 看到 unreduced `processAgent` 调用，counterexample `Dmax ≥ 2, s.resetcount ≥ 1` 收不到尾。

修（按 ChatGPT extended pro 的建议）：
1. `Protocol/RankDelta.lean` 加新 helper `processAgent_oldRc_zero_partner_true_delay_gt_one_stays`：oldRc=0、partner=true、`1 < s.delaytimer` 时 processAgent 留 Resetting（因为 dt-1≠0 杀掉了 reset guard）。
2. `propagateReset_recruits` 改写成 `unfold propagateReset processAgent` + `by_cases s.resetcount = 1` + 全 `simp`，把所有 if 一次性化简。`by_cases` 分两支：rc=1 那支用新 helper，rc≠1 那支用 `processAgent_rc_ne_zero`。

这个折腾了 5 轮才过——前几轮试 `show` + 显式 anon constructor、`set M := ...` 都被 record-update 嵌套阻挡（rw 找不到 pattern）。最后那招"unfold 全部 + simp 全部"才一气通过。

## 剩下没修的（同样根因，机械活）

`e3a8badc` 抽 `processAgent` 这个 refactor 破坏的不只 line 217 一处。BurmanProof.lean 里凡是用 `unfold propagateReset` 或 `unfold rankDeltaOSSR propagateReset` 后接 `simp + split_ifs + omega/simp_all` 的旧证明都中招了。在 line 217 没修之前这些都被 cascade 错误盖住，line 217 一通，下面这些 expose 出来。

按 grep 出来的 17 处 `unfold propagateReset` 看，至少这些还没过：

| 行（修 line 217 后）| 函数 | 错误类型 |
|---|---|---|
| 437/440/443 | `unsettled_one_step_progress` 里 `simp [C', runPairs, P, protocolPEM, Config.step, r]` | unsolved goals |
| 460 `simp only` | 同上的 `hres` 块 | whnf 超时（200000 hb 不够；试过 800000 还是超时；16M 跑了 20+ min CPU 还没回来，杀了）|
| 677 | `propagate_reset_one_step` 的 `split_ifs <;> simp_all` | simp max steps |
| 696 omega | `phase2_propagate_reset` 的 sweep filter.card ≤ rc | 我尝试加 `Finset.card_le_univ` 给 omega 喂证据但没回；我已 revert |
| 705 rewrite failed | `phase2_propagate_reset` 的 hCard rw | TBD |
| 810/816 | TBD | application type mismatch / no goals |
| 827 Unknown identifier `rankDeltaOSSR_dormant_leader_low_dt_wakes` | 可能是前向引用，需要看文件顺序 |

大部分都是同型修复：`unfold processAgent`（加在 unfold list 里）或者把 `simp_all` 升级成 `simp_all [processAgent, processAgent_rc_ne_zero]`，再就是看哪个 if 分支需要给 simp 喂哪条事实。一个一个修就行。

## 反思

1. **不要在没 build 验证的状态下连续 commit**：4.6 显然信任 LSP，但 LSP 看到的 `.olean` 跟当前源码不匹配。下次 commit 之间至少跑一次 fresh build。
2. **refactor 时跑全量 build**：`e3a8badc` 抽 processAgent 是个跨切面 refactor，本应该跑一次全量 build 看下游断没断。
3. **`unfold + simp + omega` 是脆弱的模式**：依赖 inline `let` 的展开。改成 toplevel `def` 之后这个模式就必须改写。

## 我没做但应该做的

- 没尝试在循环里跑 codex 处理这一批同型错误（每个 build 7 分钟，3-4 轮一个 fix，跑完一晚上能修完）。我做了几轮就停了，因为 16M heartbeats 那次跑到 20+ min CPU 我判断这条路不通；但其实可能是 unsettled_one_step_progress 的 `simp` 真的 pathological，应该 sorry 掉这个 simp 块换 explicit case bash。
- 没把 BurmanProof.lean 整体 sorry 化重建（保留接口、内部都 sorry）。这样至少接口约束清楚，下游可以独立验证。

## 给早上你的建议

1. `git log` 看 `dc37c600`，那两个 fix 是真的。
2. 在 editor 里打开 `BurmanProof.lean`，从 `unsettled_one_step_progress` 开始，按下游 expose 出来的错误顺序往后修，每个都是同型：unfold list 加 `processAgent`，或者把 `simp_all` 换成显式 `simp [processAgent_rc_ne_zero, processAgent_dormant_fresh_stays, processAgent_oldRc_zero_partner_true_delay_gt_one_stays]`。
3. 真的卡住的就先 sorry，标 `-- TODO(processAgent-refactor): same root cause as propagateReset_recruits`。让 build 先绿。
4. 全绿之后回到 `all_resetting_to_dormant`（task #1），那才是主线。
